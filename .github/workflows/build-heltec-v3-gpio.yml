name: Build Heltec V3 GPIO Firmware

on:
  push:
    branches:
      - meshcore-1.16-heltec-gpio-v3-work
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Build Environment
        uses: ./.github/actions/setup-build-environment

      - name: Apply Heltec GPIO patch
        run: python3 tools/patch_heltec_gpio.py

      - name: Build Heltec V3 BLE firmware
        run: pio run -e Heltec_v3_companion_radio_ble

      - name: Build Heltec V3 USB firmware
        run: pio run -e Heltec_v3_companion_radio_usb

      - name: Collect firmware
        run: |
          mkdir -p out
          cp .pio/build/Heltec_v3_companion_radio_ble/firmware.bin out/MeshCore-1.16.0-HeltecV3-GPIO-V3-BLE.bin
          cp .pio/build/Heltec_v3_companion_radio_usb/firmware.bin out/MeshCore-1.16.0-HeltecV3-GPIO-V3-USB.bin

      - name: Upload firmware artifacts
        uses: actions/upload-artifact@v7
        with:
          name: MeshCore-1.16.0-HeltecV3-GPIO-V3
          path: out/*.bin
